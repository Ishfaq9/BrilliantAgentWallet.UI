import { CustomerRechargeHistoryDto } from './customer-recharge-history-dto.model';

describe('CustomerRechargeHistoryDto', () => {
  it('should create an instance', () => {
    expect(new CustomerRechargeHistoryDto()).toBeTruthy();
  });
});
