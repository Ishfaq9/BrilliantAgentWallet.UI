import { AgentNoticeBoard } from './agent-notice-board.model';

describe('AgentNoticeBoard', () => {
  it('should create an instance', () => {
    expect(new AgentNoticeBoard()).toBeTruthy();
  });
});
